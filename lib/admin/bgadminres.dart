import 'package:flutter/material.dart';

class BgAdminRes extends StatelessWidget {
  const BgAdminRes({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}